# 工作日誌 2026-08-13

> 本次改動同步存在於 `mv-production`（工作區）與 `mv-production-web`（網頁本體），
> 兩邊 `scripts/` 原本就是逐位元組相同的兩份，改完仍保持一致。

## 起因：追 Imagen 4 的 8/17 停用期限，結果查到別的東西

開工報告把「Imagen 4 端點 2026-08-17 停用，AI Studio 專案 `gen-lang-client-0086054341`
若別處還有在用要確認」列為最急的待辦（當天剩 4 天）。全機掃了一遍。

**結論是那個期限威脅不到我們——沒有任何活的 `imagen-4.0-*` 呼叫。**
`01_MV製作` 與 `03_剪片工作流` 早在 6/16、8/3 兩次就遷完了，剩下的命中全是註解與文件。

但順著掃出真正的問題：**本 repo 的三支生圖腳本用的是 Imagen 3，而 Imagen 3 早就 404 了。**

| 檔案 | 原本用的模型 |
|---|---|
| `scripts/vertex_imagen.py` | `imagen-3.0-generate-001` |
| `scripts/imagen.py` | `imagen-3.0-generate-002` |
| `scripts/test_vertex_imagen.py` | `imagen-3.0-generate-002` |

8/3 的紀錄就寫過 `imagen-3.0-*` 回 `Publisher model ... was not found`。
也就是說這兩個 repo 的生圖功能**已經壞了一段時間，只是還沒人去踩**。

> **被通知的期限不等於真正的風險。** 花時間追 8/17 的 Imagen 4，
> 真正壞掉的卻是沒人通知的 Imagen 3。掃描時順手看一眼「還有什麼是同類的」很划算。

---

## 修法：分兩段，先路徑後模型

### 第一段：清掉寫死的路徑

4 處硬路徑（兩 repo 各 2 處）：

- `vertex_imagen.py` 的 `DEFAULT_KEY_PATH` 寫死 `I:\我的雲端硬碟\Gemini Gems\...json`
- `test_vertex_imagen.py` 的 `OUTPUT_DIR` 寫死 `I:\我的雲端硬碟\Ai Code\01_MV製作\outputs\vertex_test`

這台是 `G:` 槽，**就算模型還活著也會找不到金鑰**。改成讀環境變數
（`--key` → 環境變數 → `./.env` → `~/.vertexai.env`），輸出目錄改 repo 相對路徑。

### 第二段：遷到 Gemini

**沒有重寫，直接搬 `01_MV製作/scripts/` 那三支已經遷好、8/3 實機驗過的版本。**
檔名、CLI 參數、YAML 批次格式全部沒變，舊的呼叫方式不用改。

| 檔案 | 變成 |
|---|---|
| `vertex_imagen.py` | `generate_content()` + `gemini-3.1-flash-image`，專案／金鑰全讀環境變數 |
| `imagen.py` | 只留參數整理、YAML 迴圈、配額重試，生圖一律轉呼叫 `vertex_imagen.render()` |
| `test_vertex_imagen.py` | 預設離線 mock（不連網、不花錢），實測要加 `--live` |
| `test_vertex_imagen_config.py` | 新增，設定解析的離線測試 |

三個關鍵行為差異順帶處理掉：

1. **區域 `us-central1` → `global`**：Gemini 影像模型只開在 global，其他區域回 404
2. **一次只回一張**：Imagen 可以 `sampleCount=4`，Gemini 不行，改用迴圈補張數
3. **專案 ID** `gen-lang-client-0086054341`（死的）→ `glassy-keyword-498400-f7`（實際在用的）

兩份 YAML（`batch_example.yaml`、`mv_love_scenes.yaml`）沒寫 `model:`，
會自動吃新預設，不用改。

---

## 驗證：四層，最後有真的出圖

| 驗證 | 結果 |
|---|---|
| 離線 mock 生圖流程（`test_vertex_imagen.py`） | 通過 13、失敗 0 |
| 設定解析（`test_vertex_imagen_config.py`） | 通過 16、失敗 0 |
| 兩 repo 的 `imagen.py --check` / `vertex_imagen.py --check` | 專案／區域／模型／金鑰全正確 |
| **實機生圖（`--live`）** | **通過 2、失敗 0** |

實測產出 1024×1024 紅蘋果產品照、1.1 MB，肉眼確認畫面正常。
**這步不能省**——前三層全過只證明「設定看起來對」，只有 `--live` 能證明真的生得出圖。

測試圖落在 `outputs/vertex_test/`，`.gitignore:18` 已擋 `outputs/`，不會進 git。

---

## 踩坑

- **這台的 `python.exe` 是 Microsoft Store 空殼**。位置在
  `%LOCALAPPDATA%\Microsoft\WindowsApps\python.exe`，執行會直接 exit 49 什麼都不做，
  連 `py -m py_compile` 都跑不了（`py` 根本不存在）。
  **跑 Python 一律走 `uv run --no-project python ...`**，要套件加 `--with`。
- `~/.vertexai.env` 這台已經設好了（6/16 建的），指向
  `C:\Users\user\.credentials-vertexai-sa.json`，不是 Google Drive 裡那份。
  金鑰正本仍在 Drive 的 `Gemini Gems\`，本機這份是複本。

---

## 下一步

1. 這兩個 repo 的 `scripts/` 現在跟 `01_MV製作` 是同一份邏輯，
   **之後只改一邊會漂移**。哪天要動生圖核心，記得三邊一起同步。
2. 舊 YouTube OAuth 憑證（`client_secrets.json` / `token.json` 曾推上
   舊 `mv-production.git`）仍建議到 GCP 撤銷重發——6/17 就記了，還沒做。
3. mv-guide 正式站那筆「未命名專案」要刪，
   **得用 `shine@tmail.ilc.edu.tw` 登入才看得到**（不是平常的 `lotes@`）。

---

# 晚上這輪（家裡用筆電，22:00）

> 上面是資訊組主機 1 早上做的 Imagen 遷移，這段是晚上在家裡用筆電接續的。

## 上傳腳本 `--privacy` 預設從 private 改為 public

`剪片工作流` 的筆記寫著「兩支宜蘭課桌椅 MV 是 private，是 `lotes-video-upload`
未過 OAuth 稽核被強制鎖的，光在 Studio 點公開很可能改不動」。查下去發現這個
因果沒有證據支持——**上傳腳本自己的預設值就是 private**：

| 檔案 | 原本 |
|---|---|
| `yt-auto-publisher/scripts/upload_video.py:75` | `--privacy` `default="private"` |
| `yt-auto-publisher/scripts/batch_upload.py:49` | 同上 |

稽核限制本身屬實（2020-07-28 後建立、未過稽核的 API 專案，`videos.insert` 上傳
的影片會被鎖私人且無法申訴），**但兩個原因會產生一模一樣的結果，無從分辨**。

依使用者要求把預設改成 `public`，同步更新 `SKILL.md` 與 docstring。
語法檢查通過，AST 讀出來確認兩支都是 `public`。

> ⚠️ **之後不帶參數執行就會直接公開**，這是使用者明確要的，但操作時要有這個底。

## 這一份不要跟 `剪片工作流` 那份搞混

`剪片工作流/skills/youtube-publisher/upload_youtube.py:567` 也是
`default="private"`，而且該專案 `CLAUDE.md` 第 9 步明文要求「預設 private，
要 public 必須使用者明講並加 `--confirm-public`」。**那份沒有跟著改**，
維持保守政策。兩套腳本各自獨立，別互相套用。

## 待辦

- 到 YouTube Studio 把 v2（`Qva1SJQeZGQ`）改成公開試一次，**這一步就能判定稽核
  到底有沒有在鎖**：改得動 → 只是預設值問題；改不動 → 才需要真的送稽核。
- `mv-production-workspace` 的 `scripts/` 跟這邊是同一份邏輯，**這次 privacy
  的改動只落在這一邊**（家裡用筆電沒 clone workspace）。下次在有 workspace 的
  機器上開工要記得補，否則就是上面第 1 點警告的漂移。

## 本輪 commit

- `4d0ca10` 上傳腳本 `--privacy` 預設從 private 改為 public
